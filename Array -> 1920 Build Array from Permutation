class Solution {
public:
    vector<int> buildArray(vector<int>& nums) {
        vector <int> temp(nums.size());             //You can't equate a vector with an array
        for(int i=0; i<nums.size(); i++){
            temp[i]=nums[nums[i]];
        }
        return temp;
    }
};
